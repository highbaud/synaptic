"""Canonical vault note templates, shipped as package data.

`synaptic init` loads these; the copies under
``examples/vault/06 - SYSTEM/templates/`` are kept byte-identical (enforced by
tests/test_templates.py) so there is a single source of truth for template
content.
"""
